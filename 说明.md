


```shell
# 多版本打包

# 前提环境已经安装 goreleaser 插件

# 打包应用
goreleaser build

# 发布构建
goreleaser release
# 发布构建::deb格式
goreleaser release --snapshot --skip-publish

# 验证格式
dpkg -c dist/gwy-query_1.0.0_arm64.deb
```

```shell
# 需要创建标签
git tag v1.0.0
git push origin v1.0.0
```